"use strict"

const { listar } = require("./list")
const { encontrar } = require("./find")
const { editar } = require("./edit")
const { filtrar } = require("./filter")
const { retornar } = require("./return")

module.exports = {
	listar,
	encontrar,
	editar,
	filtrar,
	retornar,
}
